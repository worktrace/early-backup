const name = GenerateName();

class GenerateName {
  const GenerateName();
}

const lib = GenerateLib();

/// Generate a variable of library identifier of annotated element.
///
/// It's recommended to annotate it only once in a library,
/// as all annotations inside the same library
/// will provide the same value result.
class GenerateLib {
  const GenerateLib();
}
