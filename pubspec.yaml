name: worktrace_monorepo
version: 0.0.0
publish_to: none
environment: { sdk: ^3.6.0 }

workspace:
  # Application components.
  - apps/flutter
  - brand/flutter
  - packages/work_trace
  - packages/work_trace_ui

  # Shared libraries for code reuse.
  - packages/auto_stories
  - packages/avoid_nullable
  - packages/compat_utils
  - packages/custom_app
  - packages/data_gen
  - packages/inherit
  - packages/modifier
  - packages/name_gen
  - packages/smooth_ui
  - packages/state_reuse
  - packages/wrap

dependencies:
  auto_stories: ^0.0.0
